/**
 * the msg
 */
const msg = `this is blank space😏(lll￢ω￢)(￣o￣) . z Z(ˉ﹃ˉ)`

/**
 * call printf with a param
 *
 * @param str - the string to be processed
 *
 */
function hi(str) {
  console.log(str)
}

module.exports = {
  hi,
  msg
}